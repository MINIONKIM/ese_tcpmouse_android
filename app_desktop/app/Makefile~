CC = gcc
CFLAGS = `pkg-config --cflags gtk+-2.0` -g -Wall -I../include -L../lib
OUTPUT = app
OBJFILES = app.o

app : app.o
	$(CC) -o app app.o

app.o : app.c
	$(CC) $(CFLAGS) -c app.c -o app.o -lmiscfuncs -lMouseEvent -lScaleData 

clean:
	rm *.o $(OUTPUT)
